import { IntegrationSpec } from '@/components/swic/summit-sleep/IntegrationSpec';

export default function IntegrationPage() {
  return <IntegrationSpec />;
}

export const metadata = {
  title: 'SWIC Integration Specification — D365 Commerce',
};
