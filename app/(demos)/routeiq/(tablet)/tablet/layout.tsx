export default function RouteiqTabletLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen w-full bg-[#0A0A15] text-white">
      {children}
    </div>
  );
}
