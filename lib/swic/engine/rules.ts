// Re-export rules from the shared package
export { aggregateSale, evaluateRule, calculateThreshold, formatCurrency, formatCurrencyShort } from '@/lib/swic-engine';
export type { SaleAggregates } from '@/lib/swic-engine';
